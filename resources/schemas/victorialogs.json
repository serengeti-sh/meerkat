{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "query": {
      "type": "string",
      "description": "LogsQL query expression. Examples: _stream:{pod=\"my-pod\"} \"error\", _stream:{namespace=\"default\"} |~ \"error|panic\", _time:5m \"timeout\", _stream:{app=\"nginx\"} AND _time:1h \"500\""
    },
    "limit": {
      "type": "integer",
      "description": "Maximum number of log entries to return",
      "default": 50,
      "minimum": 1
    },
    "offset": {
      "type": "integer",
      "description": "Number of log entries to skip before returning results (used with limit for pagination)",
      "minimum": 0
    },
    "start": {
      "type": "string",
      "description": "Start time for the query. Accepts relative duration (e.g. '1h', '30m') or RFC3339 timestamp"
    },
    "end": {
      "type": "string",
      "description": "End time for the query (exclusive). Accepts relative duration (e.g. 'now') or RFC3339 timestamp"
    },
    "timeout": {
      "type": "string",
      "description": "Maximum query execution time (e.g. '5s', '30s')"
    }
  },
  "required": ["query"]
}
