{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "query": {
      "type": "string",
      "description": "LogQL query expression. LogQL is Grafana Loki's query language. Basic syntax: (1) Simple filter: '{app=\"nginx\"} |= \"error\"' (2) Regex filter: '{namespace=\"default\"} |~ \"err.*\"' (3) JSON parse: '{app=\"myapp\"} | json' (4) Line format: '{app=\"myapp\"} | line_format \"{{.level}}: {{.msg}}\"' (5) Aggregate: 'sum(rate({app=\"nginx\"} [5m]))'"
    },
    "limit": {
      "type": "integer",
      "description": "Maximum number of log entries to return",
      "default": 50,
      "minimum": 1
    },
    "start": {
      "type": "string",
      "description": "Start time for the query. Accepts Unix nanoseconds, RFC3339, or relative duration (e.g. '1h')"
    },
    "end": {
      "type": "string",
      "description": "End time for the query. Accepts Unix nanoseconds, RFC3339, or relative duration (e.g. 'now')"
    },
    "since": {
      "type": "string",
      "description": "Relative duration to calculate start from end (e.g. '1h', '30m'). Superseded by start if both are provided"
    },
    "step": {
      "type": "string",
      "description": "Query resolution step width for metric queries (e.g. '5m', '1h'). Only applies to matrix (metric) responses"
    },
    "direction": {
      "type": "string",
      "description": "Sort order of log results",
      "enum": ["forward", "backward"],
      "default": "backward"
    }
  },
  "required": ["query"]
}
