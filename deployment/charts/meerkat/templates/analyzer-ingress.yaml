{{- if .Values.analyzer.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "meerkat.fullname" . }}
  labels:
    {{- include "meerkat.labels" . | nindent 4 }}
  {{- with .Values.analyzer.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.analyzer.ingress.className }}
  ingressClassName: {{ .Values.analyzer.ingress.className }}
  {{- end }}
  {{- if .Values.analyzer.ingress.tls }}
  tls:
    {{- range .Values.analyzer.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.analyzer.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "meerkat.fullname" $ }}-analyzer
                port:
                  number: {{ $.Values.analyzer.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
