{{- if .Values.logs.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "meerkat.fullname" . }}-logs
  labels:
    {{- include "meerkat.labels" . | nindent 4 }}
    app.kubernetes.io/component: logs
spec:
  type: {{ .Values.logs.service.type }}
  ports:
    - port: {{ .Values.logs.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
    - port: {{ .Values.logs.service.otlpPort }}
      targetPort: otlp
      protocol: TCP
      name: otlp
  selector:
    {{- include "meerkat.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: logs
{{- end }}
